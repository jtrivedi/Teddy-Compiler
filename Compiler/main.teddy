
func squared(i: Int) -> Int {
    return i * 2;
}

func main() -> Int {
    
    let message: String = "Hello, world!";
    print(message);
    
    let i: Int = (20 + 5) * 2;
    
    if (true) {

// TODO: This will incorrectly try to print the 
// result of squared(i) as a string.
        
        print(squared(i));
    }
    
    return 0;
}
