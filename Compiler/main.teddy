
enum Expr {
    case Add        (lhs: Int, rhs: Int);
    case Subtract   (lhs: Int, rhs: Int);
    case Multiply   (lhs: Int, rhs: Int);
    case Divide     (lhs: Int, rhs: Int);
}

func squared(a: Int) -> Int {
    return a * a;
}

func main() -> Int {
    
    let i: Int = 500;
    let cat: String = "moo";
    print(cat, i);
    
    let expr: Expr = .Add(5, 10);
    
    if let expr: Expr = .Add(a, b) {
        
    }

    if (true) {
        print("moo");
    }
    
    return 0;
}
